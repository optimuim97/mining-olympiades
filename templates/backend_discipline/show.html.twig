{% extends 'base.html.twig' %}

{% block title %}Discipline{% endblock %}

{% block body %}
    <h1>Discipline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discipline.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ discipline.titre }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ discipline.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_discipline_index') }}">back to list</a>

    <a href="{{ path('app_backend_discipline_edit', {'id': discipline.id}) }}">edit</a>

    {{ include('backend_discipline/_delete_form.html.twig') }}
{% endblock %}
