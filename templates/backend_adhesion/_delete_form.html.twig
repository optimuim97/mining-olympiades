<form method="post" action="{{ path('app_backend_adhesion_delete', {'id': adhesion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adhesion.id) }}">
    <button class="btn btn-danger btn-lg">Supprimer</button>
</form>
