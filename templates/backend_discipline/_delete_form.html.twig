<form method="post" action="{{ path('app_backend_discipline_delete', {'id': discipline.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette discipline?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discipline.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
